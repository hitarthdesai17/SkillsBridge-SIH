# Phase 04: Bugs Encountered

| Bug ID | Date | Area | Symptom | Expected Behavior | Actual Behavior | Root Cause | Fix | Verification | Prevention | Lesson Learned |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-04-01 | 2026-08-19 | Simulator Test | Vitest unit test threw `TypeError: calculateReadinessScore is not a function` and `Cannot read properties of undefined (reading 'some')`. | Unit test executes `calculateOpportunityReadiness` cleanly with optional arrays. | Test runner threw TypeError during execution. | Engine function name in `readiness_engine.ts` is `calculateOpportunityReadiness` (not `calculateReadinessScore`), and array fields `projects` and `experience` required null-safe fallbacks `(candidate.projects || [])`. | Updated test import to `calculateOpportunityReadiness` and added null-safe array defaults in `readiness_engine.ts`. | Ran `npx vitest run` - 100% tests passed (13/13). | Ensure default array fallbacks in domain engines when processing optional profile fields. | Domain engine functions should be robust against missing array properties on dynamic objects. |
| BUG-04-02 | 2026-08-19 | Unit Test Suite | `auth_isolation.test.ts` timed out after 5000ms attempting network request to placeholder Supabase host. | Unit tests should execute in memory in <50ms without attempting remote network requests when environment variables are unset. | Attempted fetch to placeholder host hung until vitest timeout. | `candidate_service.ts` attempted database call even when `NEXT_PUBLIC_SUPABASE_URL` was a placeholder. | Added `isSupabaseConfigured` guard checking that URL is set and not a placeholder before calling Supabase client. | `npm test` runs all 31 tests in ~3.0s with 100% pass rate. | Always guard third-party cloud SDK calls against placeholder / test URLs in shared services. | In-memory and offline unit test performance relies on clean SDK environment detection. |


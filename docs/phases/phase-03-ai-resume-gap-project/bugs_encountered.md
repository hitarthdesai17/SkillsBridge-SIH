# Phase 03: Bugs Encountered

| Bug ID | Date | Area | Symptom | Expected Behavior | Actual Behavior | Root Cause | Fix | Verification | Prevention | Lesson Learned |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-03-01 | 2026-08-19 | Resume Parser | Fallback parser generated unsupported `TypeScript` skill and fabricated `github.com/demo/portfolio-project` URL. | Parser extracts ONLY skills, projects, and URLs explicitly present in source text (`NO EVIDENCE = NO CLAIM`). | Fallback parser matched substring `'ts'` in words like `analytics` and inserted template GitHub URL. | Keyword array contained substring `'ts'` matching non-skill words, and project fallback inserted default placeholder URLs. | Updated `fallbackResumeParser` in `src/lib/resume_parser.ts` to use strict word-boundary regex (`\btypescript\b`, `\bpython\b`), extract actual project headings, and preserve `github_url: null`. | Added negative hallucination test in `src/lib/resume_parser.test.ts` verifying unsupported skills are absent. | Enforce word-boundary regex matching and strict `null` defaults for missing metadata. | Never insert default fallback URLs or loose substring keyword matches in evidence extraction systems. |
